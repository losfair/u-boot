if TARGET_MAGICORE_DEV

config SYS_BOARD
	default "magicore-dev"

config SYS_VENDOR
	default "bluelogic"

config SYS_CPU
	default "generic"

config SYS_CONFIG_NAME
	default "magicore-dev"

config SYS_TEXT_BASE
	default 0x1c000000

config BOARD_SPECIFIC_OPTIONS # dummy
	def_bool y
	select GENERIC_RISCV
endif
